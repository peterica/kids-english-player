# 보안 정책 (Security Policy)

## 취약점 신고

**공개 이슈로 올리지 말아 주세요.**
GitHub 의 **Private vulnerability reporting** 을 이용해 주시면 됩니다.

```text
저장소 → Security 탭 → Report a vulnerability
```

개인 프로젝트라 24시간 대응은 어렵지만, 확인하는 대로 회신합니다.
재현 절차와 영향 범위를 함께 적어 주시면 빠르게 판단할 수 있습니다.

---

## 먼저 알아야 할 전제 ⚠️

이 앱은 **가정 내부 네트워크(LAN)에서 한 가정이 쓰는 것**을 전제로 설계했다.
공개 인터넷에 그대로 노출하도록 만들어지지 않았다.

| 항목 | 현재 상태 |
|---|---|
| HTTPS | **없다.** 기본 실행은 평문 HTTP 다 |
| 아이 화면 인증 | **없다.** 부모 로그인 세션을 그대로 공유한다 |
| 접근 제어 | 부모 계정 로그인 하나로 모든 화면이 열린다 |
| 요청 제한(rate limit) | 없다. 로그인 시도 횟수를 제한하지 않는다 |
| 감사 로그 | 없다 |

즉 **공유기의 포트포워딩으로 외부에 여는 순간, 위 항목이 전부 위험 요소**가 된다.

---

## 인터넷에 노출할 때 반드시 할 것

그래도 외부에서 접속해야 한다면 최소한 다음을 지킨다.

### 1. `SESSION_SECRET` 을 반드시 설정한다 (가장 중요)

값이 없으면 코드에 박힌 개발용 기본값으로 세션 쿠키를 서명한다.
이 값은 공개 저장소에 그대로 있으므로, **누구나 세션 쿠키를 위조해 로그인 없이 들어올 수 있다.**

```bash
openssl rand -hex 48
```

값이 없으면 어느 쪽으로 띄워도 멈춘다.

- `docker compose` — compose 가 기동 전에 중단하고 이유를 알려준다
- `npm start` (production) — 서버가 세션을 만들 때 오류를 던진다

개발 모드(`npm run dev`)에서만 기본값으로 동작한다. 개발 편의를 위한 것이며 운영에 쓰면 안 된다.

### 2. HTTPS 를 앞단에 둔다

Caddy · nginx · Cloudflare Tunnel 같은 리버스 프록시로 TLS 를 종료시키고,
그 뒤에 `COOKIE_SECURE="true"` 를 설정한다.

```bash
COOKIE_SECURE="true"   # HTTPS 로 서비스할 때만
```

HTTP 상태로 이 값을 켜면 쿠키가 전송되지 않아 로그인이 되지 않는다.

### 3. 강한 비밀번호를 쓴다

로그인 시도 제한이 없다. 8자 최소 길이만 강제하므로, 외부 노출 시에는 충분히 긴 값을 쓴다.
비밀번호는 scrypt(임의 salt 16바이트, 키 32바이트)로만 저장하며 평문은 남기지 않는다.

### 4. Content Library API 를 잠근다

`GET /api/content-library` 는 기본적으로 **인증 없이 조회 가능**하다(공용 콘텐츠 메타데이터만 노출).
외부에 열려 있다면 토큰을 설정한다.

```bash
CONTENT_LIBRARY_TOKEN="충분히-긴-임의-문자열"
```

### 5. 더 나은 방법 — 아예 열지 않는다

VPN(WireGuard, Tailscale 등)으로 집 네트워크에 들어와서 쓰는 편이
포트를 여는 것보다 안전하고 설정도 간단하다.

---

## 구현되어 있는 보호

없는 것만 적으면 오해가 생기므로, 있는 것도 적는다.

- **비밀번호**: scrypt + 계정별 임의 salt. 평문·가역 암호화 없음
- **세션**: HMAC-SHA256 서명 쿠키(`httpOnly`, `sameSite=lax`, 30일 만료).
  서명 위조·userId 치환·만료 토큰을 모두 거부한다 (테스트로 검증)
- **가정 간 격리**: `householdId` 를 서버 세션에서만 결정하고 클라이언트 입력을 신뢰하지 않는다.
  아이·Collection·Auto Play 세션은 항상 가정 소유를 함께 확인한다(IDOR 방지, 통합 테스트로 검증)
- **권한 분리**: ADMIN 만 Content Library 원본을 수정할 수 있고, PARENT 는 오류 신고만 보낼 수 있다.
  미인증 401 / 권한 부족 403 을 구분한다
- **로그인 실패 메시지**: 아이디와 비밀번호 중 무엇이 틀렸는지 구분해 알려주지 않는다
- **개인정보**: 이메일·실명·연락처를 수집하지 않는다 ([PRIVACY](docs/PRIVACY.md))

## 지원 범위

최신 `main` 만 지원한다. 이전 커밋으로 되돌아가 패치하지 않는다.
